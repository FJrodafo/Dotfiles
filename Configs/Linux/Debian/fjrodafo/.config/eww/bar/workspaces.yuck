;  ╦ ╦┌─┐┬─┐┬┌─┌─┐┌─┐┌─┐┌─┐┌─┐┌─┐
;  ║║║│ │├┬┘├┴┐└─┐├─┘├─┤│  ├┤ └─┐
;  ╚╩╝└─┘┴└─┴ ┴└─┘┴  ┴ ┴└─┘└─┘└─┘

(deflisten bar-workspace-0 "./scripts/WorkSpaces/Style eDP1")
(defwidget bar-workspace-0 [] (literal :content bar-workspace-0))
(defwidget bar-workspaces-widget [monitor]
  (box :orientation "v" :valign "start" :space-evenly false
    (box :visible { monitor=="eDP1" } (bar-workspace-0))
  )
)
